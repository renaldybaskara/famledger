node_modules/
dist/
.next/
build/
.turbo/
*.env
.env.local
.env.*.local
!.env.example

# Deploy secrets (generated by deploy/ scripts — never commit)
.env.railway.deployed
.env.supabase.deployed

# Expo
.expo/
ios/
android/
web-build/

# Docker
infra/volumes/

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
*.swp

# Claude Code (local tooling only — not project code)
.claude/
CLAUDE.md
mcp.json
.mcp.json

# Design assets (not source code)
Saku Design System.zip
design_handoff_saku/

# Local SQL utilities (DB-specific, not portable)
check_emails.sql
reset_email_data.sql

# ngrok (contains tunnel config — token stored in system ngrok config)
ngrok.yml
start-ngrok.ps1
